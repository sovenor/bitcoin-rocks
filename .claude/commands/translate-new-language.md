---
description: Add a brand-new locale — all ~81 i18n JSON files + config wiring
argument-hint: <language name or code>
---

You are being invoked to add a brand-new locale to bitcoin.rocks: **$ARGUMENTS**

If the locale directory `i18n/<code>/` already exists, switch to the manifest-refresh workflow instead (`/translate-manifest-refresh`).

Follow the full 7-step checklist in the workflow below — translation files, locale config, about-page count, llms.txt, llms-full.txt, memory bank, build verification.

@../../.clinerules/workflows/translate-new-language.md
